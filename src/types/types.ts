export type TContactItem = {
  imgPath: string;
  contact: string;
  alt: string;
  type: string;
}

export type TSkillItem = {
  level: number;
  name: string;
}

export type TExampleItem = {
  id: number;
  name: string;
  img: string;
  imgModal: string;
  description: string;
  href: string;
}
